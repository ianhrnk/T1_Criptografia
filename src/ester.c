#include <stdio.h>
#include "ester.h"

/*void esterInicializar(long int* n, long int* v)
{
  scanf("%ld %ld", n, v);
  if (*v < *n)
    printf("C\n");
  else
    printf("E\n");
  // Verificar se são coprimos?
}

void esterPreparar(int *bit, long int* x, long int* xb)
{
  long int r;
  scanf("%d", &bit);
  *x = fabioIniciar(n, &r);
  *xb = fabioResponder(n, s, r, bit);
  printf("C %ld %ld\n", x, xb);
}*/
