// Inicializar - recebe n, v, t | 3 <= t <= 50
void patriciaInicializar(long int*, long int*, int*, int*);
// Receber compromisso - recebe x e gera um bit aleatorio.
void patriciaRCompromisso(long int*, int*, int);
// Validar resposta - validar usando os valores de x e b.
int patriciaValidar(long int, long int, long int, long int, int, int*, int);
